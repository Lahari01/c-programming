#include <stdio.h>

int main()
{
    int n;
    printf("enter the no.");
    scanf("%d",&n);
    sum(n);
    printf("sum of nos is = %d",sum(n));
    
    return 0;
}

int sum (int n)
{
    if(n==0)
    return 0;
    else
    return (n+sum(n-1));
}
